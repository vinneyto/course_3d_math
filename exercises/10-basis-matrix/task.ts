import { Matrix3, Vector3 } from "three";

export function createBasisMatrix(
  xAxis: Vector3,
  yAxis: Vector3,
  zAxis: Vector3,
): Matrix3 {
  throw new Error("TODO: write the basis vectors into the matrix columns");
}
