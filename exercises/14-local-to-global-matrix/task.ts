import { Matrix3, Vector3 } from "three";

export function localVectorToGlobalWithMatrix(
  localVector: Vector3,
  localToGlobalMatrix: Matrix3,
): Vector3 {
  throw new Error("TODO: use the matrix to convert a copy of the vector");
}
