import { Matrix3, Vector3 } from "three";

export function multiplyMatrixByVector(
  matrix: Matrix3,
  vector: Vector3,
): Vector3 {
  throw new Error("TODO: apply the matrix to a copy of the vector");
}
